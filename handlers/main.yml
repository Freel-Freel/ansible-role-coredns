---
# handlers file for ansible-role-coredns

- name: reload_systemd
  systemd:
    daemon_reload: yes

- name: reload_service
  systemd:
    name: "{{ coredns_service_name }}"
    state: restarted

- name: reload_systemd-resolved
  systemd:
    name: systemd-resolved
    state: restarted